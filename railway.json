{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "buildCommand": "npm run build"
    },
    "deploy": {
        "startCommand": "npm run db:migrate && next start -p $PORT",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}
